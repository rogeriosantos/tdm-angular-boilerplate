// Exact copy from WebClients shared auth login component
// Minimal styling - layout handled by Tailwind classes in HTML

// Hide password reveal icon on Edge browsers
input::-ms-reveal {
  display: none;
}
